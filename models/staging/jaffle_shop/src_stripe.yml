version: 2

sources:
  - name: stripe
    database: raw        # Matches your Snowflake database
    schema: stripe        # Matches your Snowflake schema
    tables:
      - name: payment     # Matches your singular PAYMENT table name
